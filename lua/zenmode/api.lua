return {
    open = require("zenmode.zenmode").open,
    close = require("zenmode.zenmode").close,
    toggle = require("zenmode.zenmode").toggle,
    state = {
        is_enabled = require("zenmode.state").is_enabled,
    }
}
